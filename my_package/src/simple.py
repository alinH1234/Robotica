#! /usr/bin/env python
# This line specifies the interpreter used , every Python file needs to start with this line at the top.
import rospy # Import rospy, which is a Python library for ROS
rospy.init_node('Friends') # Initiate a node called ObiWan
print("We were on a break!!") # A simple Python print
